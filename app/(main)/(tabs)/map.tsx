import MapScreen from '@/app/screens/map';

// Navigation wrapper for Map screen
// This file handles the routing while the actual UI is in screens/map
// Keeps tab navigation visible while showing full-screen map
export default MapScreen;
